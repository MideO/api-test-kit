name := "api-test-kit-monitoring"

scalaVersion := "2.11.8"

organization := "com.github.mideo"

javaSource in Compile := baseDirectory.value / "src/main/java"

resourceDirectory in Compile := baseDirectory.value / "src/main/resources"
